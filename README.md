# Sales-datapipeline
I built an end-to-end sales data pipeline using Pandas for cleaning, NumPy for calculations, PySpark for scalable processing, and scikit-learn for revenue prediction. The pipeline handled 1000 records with nulls, duplicates, and feature engineering — saved output in both CSV and Parquet formats.
